# 📸 راهنمای مدیریت لوگوی سایت

## 🎯 دسترسی به تنظیمات لوگو

### مسیر:
```
داشبورد ادمین > تنظیمات > مدیریت لوگو
/admin/settings
```

---

## ✨ امکانات صفحه تنظیمات لوگو

### 1️⃣ **آپلود لوگو برای حالت روشن**
- 📁 فرمت‌های قابل قبول: PNG, JPG, SVG
- 📦 حداکثر حجم: ۲ مگابایت
- 🎨 پیشنهاد: لوگوی تیره برای پس‌زمینه روشن

### 2️⃣ **آپلود لوگو برای حالت تاریک**
- 📁 فرمت‌های قابل قبول: PNG, JPG, SVG
- 📦 حداکثر حجم: ۲ مگابایت
- 🎨 پیشنهاد: لوگوی روشن برای پس‌زمینه تاریک

### 3️⃣ **تنظیمات نمایش**
- 📏 **اندازه لوگو:** ۳۲ تا ۸۰ پیکسل (Slider)
- 📝 **نام سایت:** فارسی و انگلیسی
- 💬 **تگ‌لاین/شعار:** فارسی و انگلیسی

### 4️⃣ **پیش‌نمایش لحظه‌ای**
- 👁️ مشاهده نمایش لوگو در هدر
- ⚡ به‌روزرسانی Real-time
- 🎭 پیش‌نمایش هر دو حالت (روشن/تاریک)

---

## 🛠️ نحوه استفاده

### گام 1: ورود به داشبورد
```
1. وارد پنل ادمین شوید
2. از منوی سمت راست، روی "تنظیمات" کلیک کنید
```

### گام 2: آپلود لوگو
```
1. در بخش "لوگوی حالت روشن" یا "لوگوی حالت تاریک"
2. روی دکمه "انتخاب فایل" کلیک کنید
3. فایل لوگو را انتخاب کنید
4. پیش‌نمایش لوگو نمایش داده می‌شود
```

### گام 3: تنظیمات
```
1. اندازه لوگو را با Slider تنظیم کنید
2. نام سایت و تگ‌لاین را وارد کنید
3. در بخش "پیش‌نمایش در هدر" نتیجه را ببینید
```

### گام 4: ذخیره
```
1. روی دکمه "ذخیره تنظیمات" کلیک کنید
2. لوگو در هدر سایت به‌روزرسانی می‌شود
```

---

## 📊 توصیه‌های طراحی لوگو

### ✅ بهترین Practices:
- **اندازه بهینه:** ۵۱۲×۵۱۲ پیکسل (مربع)
- **فرمت:** PNG با پس‌زمینه شفاف (Transparent)
- **رنگ‌ها:** 
  - حالت روشن: لوگوی تیره یا رنگی
  - حالت تاریک: لوگوی سفید یا روشن
- **سادگی:** لوگو باید در اندازه‌های کوچک هم خوانا باشد

### ⚠️ اجتناب از:
- ❌ فایل‌های بزرگ‌تر از ۲MB
- ❌ لوگوهای با جزئیات زیاد
- ❌ فونت‌های کوچک در لوگو
- ❌ رنگ‌های کم‌کنتراست

---

## 🎨 نمونه‌های پیشنهادی

### گزینه 1: حرف B ساده
```
حرف B با:
- Gradient رنگی
- Border سفید
- Diamond طلایی در گوشه
```

### گزینه 2: لوگوی متنی
```
نام "بی لوک" با:
- فونت Bold
- Gradient Primary
```

### گزینه 3: آیکون + متن
```
آیکون (الماس، تاج، قلب) + نام سایت
```

---

## 🔄 استفاده از لوگو در کد

### نمایش لوگوی آپلود شده در Header:
```tsx
// در Header.tsx
{logo ? (
  <img 
    src={logo} 
    alt="بی لوک"
    width={logoSize}
    height={logoSize}
  />
) : (
  <BelookLogo variant="gradient" size={logoSize} />
)}
```

### نمایش بر اساس Theme:
```tsx
const logo = theme === 'dark' ? logoDark : logoLight;
```

---

## 📝 API Endpoints (برای توسعه‌دهندگان)

### آپلود لوگو:
```javascript
POST /api/admin/settings/logo
Content-Type: multipart/form-data

{
  logo: File,
  mode: 'light' | 'dark'
}
```

### دریافت تنظیمات:
```javascript
GET /api/admin/settings/logo

Response:
{
  logo: string,
  logoDark: string,
  siteName: string,
  siteNameEn: string,
  tagline: string,
  taglineEn: string,
  logoSize: number
}
```

### ذخیره تنظیمات:
```javascript
POST /api/admin/settings/save
{
  logo: string,
  logoDark: string,
  siteName: string,
  ...
}
```

---

## 🎯 تکمیل شده ✅

صفحه تنظیمات لوگو با امکانات زیر آماده است:
- ✅ آپلود لوگو برای دو حالت (روشن/تاریک)
- ✅ پیش‌نمایش لحظه‌ای
- ✅ تنظیم اندازه با Slider
- ✅ ویرایش نام و تگ‌لاین سایت
- ✅ حذف و تغییر لوگو
- ✅ بازنشانی تنظیمات
- ✅ رابط کاربری شیک و مدرن

---

## 📍 مسیر فایل‌ها:
```
/src/app/[locale]/admin/settings/page.tsx
/src/components/ui/label.tsx (جدید)
```

برای مشاهده صفحه:
👉 وارد پنل ادمین شوید و روی "تنظیمات" کلیک کنید
